export declare enum BookingStatus {
    PENDING = "PENDING",
    CONFIRMED = "CONFIRMED",
    CANCELLED = "CANCELLED",
    FAILED = "FAILED"
}
export declare enum PriceType {
    FIXED = "FIXED",
    PER_UNIT = "PER_UNIT"
}
