export enum BookingStatus {
    PENDING = "PENDING",
    CONFIRMED = "CONFIRMED",
    CANCELLED = "CANCELLED",
    FAILED = "FAILED"
}

export enum PriceType {
    FIXED = "FIXED",
    PER_UNIT = "PER_UNIT"
}