export enum BookingStatus {
    PENDING = "PENDING",
    CONFIRMED = "CONFIRMED",
    FAILED = "FAILED",
    CANCELLED = "CANCELLED",
    COMPLETED = "COMPLETED"
}

export enum PriceType {
    FIXED = "FIXED",
    PER_UNIT = "PER_UNIT"
}